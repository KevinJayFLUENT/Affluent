node_modules/
dist/
.env

# Local database (seeds migrate in on first run; see server/store.js)
server/data/db.json
# Legacy insights store (pre-Phase-3; now lives in db.json)
server/data/dashboards.store.json
